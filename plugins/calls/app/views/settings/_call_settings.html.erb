<h2>Call Settings</h2>
<div class="call settings">
  <fieldset class="box tabular">
  <legend>Twilio</legend>
    <p>
      <label for="twilio_sid">Twilio sid<span class="required"> *</span></label>
      <input type="text" id="settings_twilio_sid" size="40" value="<%= settings['twilio_sid'] %>" required name="settings[twilio_sid]" >
    </p>
    <p>
      <label for="twilio_token">Twilio token<span class="required"> *</span></label>
      <input type="text" id="settings_twilio_token" size="40" value="<%= settings['twilio_token'] %>" required name="settings[twilio_token]" >
    </p>
    <p>
      <label for="twilio_phone_number">Twilio Phone Number<span class="required"> *</span></label>
      <input type="text" id="settings_twilio_phone_number" size="40" value="<%= settings['twilio_phone_number'] %>" required name="settings[twilio_phone_number]" >
      <em class="info">Twilioに登録した電話番号を国際表記(先頭'+1')で登録してください。</em>
    </p>
    <p>
      <label for="twilio_Respons URL">Twilio Resposn URL<span class="required"> *</span></label>
      <input type="text" id="settings_twilio_respons_url" size="40" value="<%= settings['twilio_respons_url'] %>" name="settings[twilio_respons_url]" >
    </p>
  </fieldset>
  <fieldset class="box tabular">
  <legend>Escalation Rule</legend>
    <p>
      <label for="Timeout">Timeout<span class="required"> *</span></label>
      <input type="number" id="settings_timeout" size="40" value="<%= settings['timeout'] %>" min="1" max="180" required name="settings[timeout]" >
      <em class="info">一人あたりのコール時間です。1～180の値を登録してください</em>
    </p>
    <p>
      <label for="Max Loop Count">Max Loop Count<span class="required"> *</span></label>
      <input type="number" id="settings_max_loop_count" size="40" value="<%= settings['max_loop_count'] %>" min="1" max="5" required name="settings[max_loop_count]" >
      <em class="info">エスカレーションのループ回数の上限値です。1～5の値を登録してください。</em>
    </p>
  </fieldset>
  <fieldset class="box tabular">
  <legend>Escalation User</legend>
    <%= render :template => 'escalation_users/index' %>
  </fieldset>
</div>
